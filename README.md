# School Backend

Серверная часть приложения (Node.js).

## 🛠 Установка

Перед запуском убедитесь, что у вас установлены Node.js и Redis.

Установите необходимые зависимости проекта:

```bash
npm install express body-parser cors axios ioredis dotenv pm2
```

## 🚀 Запуск

Для запуска приложения в фоновом режиме используется менеджер процессов PM2.

Запустите сервер следующей командой:

```bash
pm2 start index.js --name "school-backend"
```

## 🔄 Управление процессом

### Перезапуск
Если вы внесли изменения в код или обновили конфигурацию, перезапустите сервер:

```bash
pm2 restart school-backend
```

### Другие полезные команды PM2

Остановка сервера:
```bash
pm2 stop school-backend
```

Просмотр логов:
```bash
pm2 logs school-backend
```

Просмотр статуса всех процессов:
```bash
pm2 list
```
